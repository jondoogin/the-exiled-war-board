# Clash Royale developer API key from https://developer.clashroyale.com
# The key is locked to the IP addresses you list when you create it.
CR_API_TOKEN=

# Your clan tag, with or without the leading #
CR_CLAN_TAG=#2PP0YQRV

# Base URL. Use the RoyaleAPI proxy when your host has no fixed IP:
#   https://proxy.royaleapi.dev/v1   (whitelist 45.79.218.79 on the key)
# Use the direct API when you run from a machine with a static IP:
#   https://api.clashroyale.com/v1
CR_API_BASE=https://proxy.royaleapi.dev/v1

PORT=5180
